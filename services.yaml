apiVersion: v1
kind: Service
metadata:
 name: webapp
spec:
 selector:
  app: webapp
 
 ports:
  - name: http
    port: 80
    nodePort: 30080

 type: nodePort



apiVersion: v1
kind: Service
metadata:
 name: application-queue 
spec:
 selector:
  app: queue
 ports:
  - name: http
    port: 8161
    nodePort: 30011
  
  - name: endpoint
    port: 61616
    

 type: NodePort
